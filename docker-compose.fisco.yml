# DEPRECATED（v0.18+）：FISCO 已是 docker-compose.yml 默认，无需本 overlay。
#
# 旧命令（仍可用，等价于 make up）：
#   docker compose -f docker-compose.yml -f docker-compose.fisco.yml up -d
#
# MiniLedger 回退：docker-compose.legacy.yml + --profile legacy
# 详见 docs/fisco-bcos-migration.md

name: smart-ledger

# 保留空 services 块以兼容仍引用本文件的脚本；合并后无额外变更。
services: {}
