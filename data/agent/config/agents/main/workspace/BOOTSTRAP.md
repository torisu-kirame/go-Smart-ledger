# Bootstrap

首次启动时的初始化说明（Smart Ledger 集成）。

1. 确认 OpenClaw Gateway 已运行（`make openclaw-gateway-up` 或 `make offline-ai-up`）
2. 在控制台 **设置 → AI** 填写模型 API Key 并 **测试连接**
3. 在 **AI 助手** 页编辑下方 Agent 设定文件（AGENTS、SOUL 等）
4. 可选：同步账本上下文后提问
